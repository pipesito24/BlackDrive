spring:
  application:
    name: financiamiento-service
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false